---
import { recipes } from '../data/recipes.ts';
---

<html>
<head>
  <title>malaika's cookbook</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Ctext y='.9em' font-size='90'%3E%E2%AD%90%3C/text%3E%3C/svg%3E" />
  <style>
    body { 
      font-family: Georgia, serif;
      max-width: 1200px; 
      margin: 0 auto; 
      padding: 60px 20px;
      line-height: 1.6;
      color: #333;
      background: #fff;
    }
    .nav-links {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
    }
    .back-link, .nick-link {
      color: #666;
      text-decoration: underline;
      font-size: 0.9rem;
    }
    .back-link:hover, .nick-link:hover {
      color: #333;
    }
    h1 { 
      font-size: 1.5rem;
      font-weight: normal;
      margin-bottom: 20px;
      text-align: center;
      letter-spacing: 0.5px;
    }
    .subtitle {
      text-align: center;
      font-style: italic;
      color: #666;
      margin-bottom: 60px;
      font-size: 0.9rem;
    }
    .recipe-list {
      list-style: none;
      padding: 0;
    }
    .recipe-item {
      display: flex;
      align-items: center;
      padding: 15px 0;
      border-bottom: 1px solid #ddd;
      gap: 20px;
    }
    .recipe-item:last-child {
      border-bottom: none;
    }
    .recipe-name {
      flex: 1;
    }
    .recipe-name a {
      transition: filter 0.3s ease;
      text-decoration: none;
      color: #4a7c59;
      font-weight: normal;
      line-height: 1.2;
      font-size: 2rem;
    }
    .recipe-name a:hover {
      filter: blur(2px);
    }
    .recipe-meta {
      margin-left: 20px;
      flex: 1;
      color: #666;
      font-size: 0.9rem;
      line-height: 1.4;
    }
    .recipe-meta .location {
      font-weight: normal;
    }
    .recipe-meta .type {
      color: #999;
    }
    .recipe-images {
      width: 124px;
      min-width: 124px;
      display: flex;
      gap: 4px;
    }
    .recipe-image img{
      transition: transform 0.2s ease;
      width: 60px;
      height: 60px;
      background: #f0f0f0;
      border-radius: 4px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.7rem;
      color: #999;
    }
    .recipe-date {
      color: #666;
      font-size: 0.9rem;
      min-width: 80px;
      text-align: right;
    }
    .modal {
      display: none;
      position: fixed;
      z-index: 2000;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
    }
    .modal-content {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      max-width: 90%;
      max-height: 90%;
    }
    .modal img {
      width: 100%;
      height: auto;
      border-radius: 4px;
    }

    /* Tablet styles */
    @media (max-width: 768px) {
      body {
        padding: 40px 15px;
      }
      .recipe-item {
        flex-direction: column;
        align-items: flex-start;
        gap: 10px;
        padding: 20px 0;
      }
      .recipe-name {
        min-width: auto;
        width: 100%;
      }
      .recipe-name a {
        font-size: 1.5rem;
      }
      .recipe-meta {
        margin-left: 0;
        width: 100%;
      }
      .recipe-images {
        width: 100%;
        min-width: auto;
        justify-content: flex-start;
      }
      .recipe-date {
        min-width: auto;
        text-align: left;
        width: 100%;
      }
    }

    /* Mobile styles */
    @media (max-width: 480px) {
      body {
        padding: 20px 10px;
      }
      h1 {
        font-size: 1.2rem;
      }
      .subtitle {
        font-size: 0.8rem;
        margin-bottom: 40px;
      }
      .recipe-name a {
        font-size: 1.2rem;
      }
      .recipe-meta {
        font-size: 0.8rem;
      }
      .recipe-image {
        width: 50px;
        height: 50px;
        font-size: 0.6rem;
      }
      .modal-content {
        padding: 15px;
        max-width: 95%;
        max-height: 95%;
      }
    }

    /* Large desktop styles */
    @media (min-width: 1400px) {
      body {
        max-width: 1400px;
        padding: 80px 40px;
      }
      .recipe-name a {
        font-size: 2.2rem;
      }
      .recipe-meta {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="nav-links">
    <a href="https://malaikaaiyar.me" class="back-link">back to main site</a>
    <a href="https://recipes.marsh.blue" class="nick-link">to nick's site</a>
  </div>
  <h1># malaika's cookbook</h1>
  <p class="subtitle">for mom and dad, who have been taking food to the next level my whole life. <br> these recipes range from experiments at home to food that i had somewhere else that changed my life that i had to recreate. i mostly eat keto/high-protein, i mostly cook for vegetarians, so there will be some recurring themes! hopefully everything is tasty nonetheless :)</p>
  
  <ul class="recipe-list">
    {recipes.map(recipe => (
      <li class="recipe-item">
        <div class="recipe-name">
          <a href={`/recipes/${recipe.slug}`}>{recipe.name}</a>
        </div>
        <div class="recipe-meta">
          <div class="location">{recipe.cuisine}</div>
          <div class="type">{recipe.type}</div>
        </div>
        <div class="recipe-images">
          {recipe.images.map(image => (
            <div class="recipe-image" data-image={image}><img src={image} /></div>
          ))}
        </div>
        <div class="recipe-date">{recipe.date}</div>
      </li>
    ))}
  </ul>

  <div id="imageModal" class="modal">
    <div class="modal-content">
      <img id="modalImage" src="" alt="">
    </div>
  </div>

  <script>
    const modal = document.getElementById("imageModal");
    const modalImg = document.getElementById("modalImage");

    document.querySelectorAll(".recipe-image").forEach(img => {
      img.addEventListener("mouseenter", function() {
        modal.style.display = "block";
        modalImg.src = this.getAttribute("data-image");
      });

      img.addEventListener("mouseleave", function() {
        modal.style.display = "none";
      });
    });
  </script>
</body>
</html>
